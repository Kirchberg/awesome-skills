interface:
  display_name: "VoiceOver Accessibility"
  short_description: "Build and verify VoiceOver-ready iOS interfaces"
  default_prompt: "Use $voice-over-accessibility to fix this iOS flow: apply source-evident semantic corrections now, then report any manual VoiceOver verification still pending."

policy:
  allow_implicit_invocation: true
