interface:
  display_name: "SwiftUI Optimization"
  short_description: "Tune SwiftUI updates, scrolling, and memory"
  default_prompt: "Use $swiftui-optimization to refactor this SwiftUI view: apply safe source-proven corrections now, and profile only runtime regressions, trade-offs, and performance claims."

policy:
  allow_implicit_invocation: true
