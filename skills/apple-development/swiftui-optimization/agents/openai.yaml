interface:
  display_name: "SwiftUI Optimization"
  short_description: "Tune SwiftUI updates, scrolling, and memory"
  default_prompt: "Use $swiftui-optimization to build or refactor this SwiftUI view for correct, measurable update, scrolling, and resource-lifetime performance."

policy:
  allow_implicit_invocation: true
