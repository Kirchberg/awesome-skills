interface:
  display_name: "Swift iOS Performance"
  short_description: "Optimize Swift code for CPU and memory"
  default_prompt: "Use $swift-ios-performance to review and improve this Swift code with measurement-backed CPU and memory optimizations."

policy:
  allow_implicit_invocation: true
