interface:
  display_name: "Swift Concurrency"
  short_description: "Design and migrate safe Swift concurrency"
  default_prompt: "Use $swift-concurrency to review this Swift code for isolation, task lifetime, cancellation, and performance issues, then propose a verified fix."

policy:
  allow_implicit_invocation: true
