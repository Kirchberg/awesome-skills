interface:
  display_name: "GitHub PR Review Monitor"
  short_description: "Loop on connector PR feedback"
  default_prompt: "Use $github-pr-codex-review-monitor to monitor this PR until connector feedback and required checks are clear."

policy:
  allow_implicit_invocation: true
