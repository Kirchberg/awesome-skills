interface:
  display_name: "GitHub PR Review Monitor"
  short_description: "Loop on connector PR feedback"
  default_prompt: "Use $github-pr-codex-review-monitor to monitor this PR until Codex feedback and required checks are clear, or stop after 60 minutes of Connector silence without claiming approval."

policy:
  allow_implicit_invocation: true
