interface:
  display_name: "Docs Feature Collect"
  short_description: "Collect feature evidence, then document it"
  default_prompt: "Use $docs-feature-collect to document a shipped feature from its tracker item and change requests."

policy:
  allow_implicit_invocation: true
